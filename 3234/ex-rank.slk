
// (1) invariant preservation 
checkentail  i<=n & i!=n |- (i+1)<=n.

// (2) rank is bounded
checkentail  i<=n & i!=n |- n-i>=0.

// (3) rank is decreasing
checkentail  R0=(n-i) & i<=n & i!=n  |- R0>(n-(i+1)).

