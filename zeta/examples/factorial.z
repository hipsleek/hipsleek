let [induction n] fact(n)
such that
n < 0 -> fact(n) = 0;
fact(0) = 1;
n > 0 -> fact(n) = n * fact(n-1).

theorem n >= 0 -> fact(n) > 0.

//theorem 0 <= n & n <= m -> divides(fact(n),fact(m)).

//theorem n != 2 -> ! isprime(fact(n)).
