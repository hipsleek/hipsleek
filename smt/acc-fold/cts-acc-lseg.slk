data node {
    int val;
    node next
}.

pred lseg<p> == self = null
    or self::node<_,r> * r::lseg<p> & self != null.


checkentail_exact
    x::node<_,p> * p::node<_,q> & x != null
        |- x::lseg<q>.
print residue.
expect Valid.

checkentail_exact
    x::node<_,p> * p::node<_,q> * q::node<_,u> * u::node<_,v>
        |- x::lseg<v>.
print residue.
expect Valid.
// found Fail??

checkentail_exact
    x::lseg<q> |- x::node<_,p> * p::node<_,q> & x != null.
