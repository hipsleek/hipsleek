relation odd(int n) == (n = 1 | n > 1 & exists (m: n=m+1 & even(m))).

relation even(int n) == (n = 0 | n > 0 & exists (m: n=m+1 & odd(m))).

checkentail true |- odd(3).

checkentail true |- odd(4).

checkentail odd(n) |- odd(n+2).

checkentail odd(n) |- even(n+2).
