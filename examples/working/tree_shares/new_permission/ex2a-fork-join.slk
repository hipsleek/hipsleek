data cl {int val;}.

macro L == (#,) .
macro R == (,#) .
macro LL == ((#,),).
macro LR == ((,#),).
macro RL == (,(#,).
macro RR == (,(,#).

 data node { int val ; node next }.

/*
pred ll<n> == self = null & n = 0
	or self::node<next = r> * r::ll<n - 1>
	inv n.
*/
/*
checkentail x::node<_,q> |-  x::node<_,q>.
print residue.
expect Valid.
*/

checkentail x::node(v)<_,q> |-  x::node(v)<_,q>.
print residue.
expect Valid.
