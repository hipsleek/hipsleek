float fabs(float x)
  requires Term[]
  ensures res = __abs(x);

void qfloat_incbet_392()
{
  float u,n,b,x,v,z,t,a,s;
  while (fabs(v) > z)
  {
    u = (n - b) * x / n;
    t *= u;
    v = t / (a + n);
    s += v; 
    n += 1.0;
  }
}
